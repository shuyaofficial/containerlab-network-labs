# Grafana に Loki を datasource として自動プロビジョン。
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://localhost:3100
    isDefault: true
    editable: true
    jsonData:
      maxLines: 1000
