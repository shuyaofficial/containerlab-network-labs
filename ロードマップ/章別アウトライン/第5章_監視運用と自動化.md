# 第5章: 監視・運用・自動化（⬜）

> 「作って終わり」ではなく「運用できる」状態へ。監視（見える化）と自動化（人手削減）。
> この章は既存の clab 資産（`ansible-inventory.yml` / `nornir-simple-inventory.yml`）が活きる。

---

## 🎯 章の狙い
- SNMP/Syslog/NetFlow で機器とトラフィックを「見える化」できる
- NETCONF/RESTCONF + YANG で、構造化データによる設定投入/取得ができる
- Ansible/nornir で複数機器へ設定を一括・冪等に流せる

## 前提
- 第1〜4章（監視/自動化の対象となる構成が一通り組めること）
- 機器へのSSH有効化（`crypto key generate rsa` 等。作業ログ §3 のSSH未設定メモ参照）

---

## 📋 テーマ一覧

| # | タイトル | 狙い | 対応RFC | 難易度 | 到達条件 |
|---|---|---|---|---|---|
| 16 | SNMP / Syslog / NetFlow | SNMPポーリング/トラップ、Syslog集約、フロー可視化 | `rfc3411` `rfc3412` `rfc3416` | 中級 | NMSから機器をポーリング、障害時トラップ受信、Syslogをサーバ集約 |
| 17 | NETCONF / RESTCONF / YANG / Ansible | XML/JSON構造データで設定、YANGモデル理解、Ansibleで一括投入 | `rfc6241` `rfc6242` `rfc7950` `rfc8040` | 上級 | NETCONFで設定変更、Ansible playbookで複数ノードへ冪等適用 |

---

## 🔑 つまずきポイント先回り
- **SNMP**: v2c(community) と v3(認証/暗号) の違い、OID/MIBの読み方
- **NETCONF**: candidate/running/startup データストア、commit/lock
- **Ansible**: 冪等性、`ios_config` と純正モジュール、インベントリ（既存ファイルを流用）

## 🔗 既存資産の活用
- 各 `clab-*/ansible-inventory.yml`・`nornir-simple-inventory.yml` をそのまま教材に。
- まず「全機器の `show version` を一括取得」から始めると自動化の効果を体感できる。

## ✅ 章末チェック
- [ ] SNMPv3 で安全にポーリングでき、トラップを受けられる
- [ ] Syslog の severity を理解し、必要レベルだけ集約できる
- [ ] NETCONF の candidate→commit の流れを説明し、実行できる
- [ ] Ansible で複数機器へ冪等に設定投入できる
- [ ] 各テーマの `振り返りシート` 作成済み
