# 構築ログ: 社内LAN更改・移行・移設

## 日付: 202X-XX-XX
## 実施者: 

### 1. トポロジのデプロイ
```bash
# 実行コマンドと結果
```

### 2. 現行環境as-is再現（Mission 1）
```text
# old-rt

# old-core

# old-sw1

# old-sw2

# mon-core

# cap-sw（SPAN設定含む）

# SNMPv3ポーリング確認・v2c失敗確認

```

#### 移行前ベースライン記録
- IF棚卸し（各機器のインタフェース一覧・VLAN割当）:

- 経路表（`show ip route`）:

- ARPテーブル:

##### ping疎通マトリクス
| 送信元＼宛先 | 203.0.113.1 | 自VLAN GW | pc-a | pc-b | srv-file |
|---|---|---|---|---|---|
| pc-a | | | ― | | |
| pc-b | | | | ― | |
| srv-file | | | | | ― |
| nms（監視セグメント経由） | | | | | |

### 3. 移行計画策定（Mission 2）
```text
# 移行方式比較（一括切替／段階移行／並行稼働）の要点と、段階移行を採用する理由（自分の言葉で）

# カットオーバーMOPの空欄を埋めた際のメモ

# 切り戻し手順書の空欄を埋めた際のメモ

```

### 4. 新環境事前構築（Mission 3）
```text
# new-core1（VLAN・trunk・SVI・HSRP・STP root primary）

# new-core2（VLAN・trunk・SVI・HSRP・STP root secondary）

# new-rt（OSPF area0・default-information originate・Lo0=203.0.113.2）

# フロアSW向けポート／SVIがshutdownのままであることの確認

# HSRPフェイルオーバー単体試験結果（Active shutdown→Standby昇格→preempt復帰）

# nms連続ping結果（新環境構築中、既存端末の通信に無影響であることの確認）

```

### 5. カットオーバー第1弾（VLAN10）（Mission 4）
```text
# 新旧トランク開通・STP収束確認

# old-core VLAN10 SVI shutdown → new-core1/new-core2 VLAN10 SVI no shutdown（HSRP有効化）

# linkDown等トラップ受信ログ（証跡）

# カットオーバーMOPへの実施時刻・実測断時間の記入

```

### 6. 切り戻し訓練→再カットオーバー（VLAN20/30＋WAN）（Mission 5）
```text
# VLAN20 初回カットオーバー

# 切り戻し判断基準抵触の想定・切り戻し手順書どおりの復旧（旧SVI no shutdown・新SVI shutdown）

# 復旧確認

# VLAN20 再カットオーバー（成功）

# VLAN30 カットオーバー

# old-core default依存解消・WANのnew-rt系統への切替（pc-a→203.0.113.2到達確認）

```

#### 断時間測定記録
| VLAN | 開始時刻 | 終了時刻 | 損失パケット数 | 断時間（損失数×0.2秒） | 判定（60秒以内） |
|---|---|---|---|---|---|
| 10 | | | | | |
| 20（初回） | | | | | |
| 20（再実施） | | | | | |
| 30 | | | | | |

### 7. 移設・撤去・移行後総合試験（Mission 6）
```text
# pc-b移設（old-sw1側ポートshutdown → pc-b内eth1→eth2付替え → old-sw2側ポートaccess VLAN20化）

# new-core1/new-core2/new-rtのSNMPv3監視追加

# mon-core defaultのnew-core1(10.29.0.2)への切替

# old-core/old-rt shutdown → docker stop（撤去）

```

#### 移行後 全点疎通マトリクス
| 送信元＼宛先 | 203.0.113.2 | 自VLAN GW（HSRP VIP） | pc-a | pc-b | srv-file |
|---|---|---|---|---|---|
| pc-a | | | ― | | |
| pc-b | | | | ― | |
| srv-file | | | | | ― |
| nms（SNMPv3監視、new-core1/2/new-rt含む） | | | | | |

### 8. トラブルシューティングメモ
- [発生した問題]
- [原因と解決策]
