---
type: feasibility
theme: "ZERO_zero_trust"
status: draft
date: 2026-07-04
tags: [zero-trust, feasibility, arm64, verification, oss]
title: "テーマZERO｜arm64 軽量検証計画"
---

# テーマZERO｜arm64 軽量検証計画

Phase 1-6 をデプロイせずに、各観点の代替 OSS が **arm64 で動くか** を軽量に確かめる計画。
判定結果は別タスクT7が `03_詳細設計/軽量検証結果_2026-07-04.md`（type: feasibility）に記録し、[実装可能性マトリクス](../02_基本設計/実装可能性マトリクス.md) の確度列を確定値に更新する。

> **本計画は L7 トラック（Phase 0-6）の OSS が対象。** NW-ZT トラック（N1-N4: NAC/SDP-ZTNA/NDR/μセグ）の arm64 検証は別計画 [軽量検証計画_nwzt](軽量検証計画_nwzt.md) に分離している。

## 検証の目的とスコープ

- 目的: OSS 各イメージの **arm64 マニフェスト有無** と、要となる機能（起動・SSL bump 等）の**実現見込み**を確認する。
- スコープ内: manifest 確認（静的判定）＋ 必要に応じた pull/起動の実検証（軽量）。
- スコープ外: Phase 1-6 の本格デプロイ・機能結合試験（見込み確認で十分というユーザー決定）。
- 前提: VM `clab`（Ubuntu 24.04 / arm64）。IOL 非連携。

## 判定基準

| 判定 | 条件 | 次のアクション |
|---|---|---|
| **High** | 公式 arm64 manifest が有る（`docker manifest inspect` に `arm64` が含まれる） | 採用確定。実検証は起動確認のみ |
| **Med** | manifest は有るが起動/機能に不確実性（SSL bump・重量・posture 等） | pull して起動・軽量機能確認を実検証 |
| **Low** | arm64 manifest が無い or 明示非対応 | 代替差し替えルールを適用 |

manifest 確認コマンド（設計値。実行はT7）:

```bash
# arm64 対応の静的確認（イメージを pull せずに判定）
docker manifest inspect <image> | grep -i 'arm64'
```

## 検証対象イメージ一覧

観点ごとの確定案・代替案を含む。arm64 見込みは検証前の想定で、確定はT7の結果ファイルによる。

| # | 観点 | イメージ（役割） | arm64 見込み | 判定見込み | 検証方法 |
|---|---|---|---|---|---|
| 1 | ID統制 | keycloak（IdP） | 有（公式 arm64 提供） | High | manifest + 起動確認 |
| 1 | ID統制（発展） | osixia/openldap（ユーザー DB 発展） | 一部要確認 | Med | manifest 確認 |
| 3 | NWセキュリティ | pomerium（IAP） | 有 | High | manifest + 起動確認 |
| 3 | NWセキュリティ | oauth2-proxy（IAP 起点） | 有 | High | manifest + 起動確認 |
| 6 | SIEM 可視化 | grafana | 有（公式 arm64） | High | manifest + 起動確認 |
| 6 | SIEM 保存 | loki | 有（公式 arm64） | High | manifest + 起動確認 |
| 6 | SIEM 収集 | promtail | 有（公式 arm64） | High | manifest + 起動確認 |
| 4/5 | WEB+DLP | mitmproxy | 有 | High | manifest + SSL bump / アドオン軽量確認 |
| 2 | デバイス統制 | step-ca（smallstep） | 有 | Med | manifest + mTLS 発行フロー確認 |
| 4 | WEB 代替 | squid（SSL bump） | 一部要確認 | Med | manifest + SSL bump 可否 |
| 4 | WEB 代替 | suricata（IDS） | 一部要確認 | Med | manifest + 起動確認 |
| 6 | SIEM 発展 | wazuh-manager | 要確認・重量 | Med/Low | manifest + メモリ実測 |
| 5 | DLP 代替 | clamav（AV） | 一部要確認 | Med | manifest 確認 |
| 3 | NW 発展 | openziti（ZTNA 発展） | 要確認 | Med | manifest 確認 |
| 2 | デバイス補助 | osquery | 一部要確認 | Med | manifest 確認 |

> 上表は設計時の見込み。T7が manifest と軽量起動を実施し、判定を確定値に更新する。

## 代替差し替えルール

Low（arm64 非対応）または重量ブロッカーが出た場合の差し替え方針。基本設計書の設計判断（D-2 / D-4）と整合させる。

| 対象 | 事象 | 差し替え | 根拠 |
|---|---|---|---|
| SIEM | Wazuh が arm64 不可 or 重すぎる | **Loki 一本**（Grafana+Loki+Promtail のみ） | D-4。SIEM は早期投入が目的で、軽量が優先 |
| WEB | squid の SSL bump が arm64 で成立しない | **mitmproxy 一本化**（SWG を mitmproxy に集約） | D-2。同一経路で検査・DLP を兼務 |
| WEB IDS | suricata が arm64 不可 | mitmproxy のログ + Loki 側の検知ルールで代替 | IDS 発報の目的（SIEM へ通知）は維持 |
| DLP | c-icap + ClamAV が arm64 不可 | **mitmproxy DLP アドオンに一本化** | D-2。DLP は SWG の通信を検査する同一経路上 |
| ID発展 | OpenLDAP が不可 | Keycloak 内蔵 DB のまま（OpenLDAP 連携は見送り） | 発展課題。今回は内蔵 DB で完結 |
| デバイス | posture の実収集が難しい | posture claim を **モック**（固定属性で分岐を示す） | 要件 FR-2。実装可能性の確認が目的 |

差し替えの優先原則:

1. **arm64 で動くこと**を最優先（NFR-1）。動かないものは代替する。
2. **観点の目的を維持**する（例: IDS 不可でも「検知→SIEM 通知」の導線は残す）。
3. **軽量・集約を選ぶ**（重量級は Loki 一本・mitmproxy 一本化へ寄せる）。

## 検証手順（T7 が実施）

1. 上表の各イメージに `docker manifest inspect` を実行し、arm64 有無を記録する。
2. Med 判定のものは pull → 起動 → 要機能の軽量確認（Keycloak 起動 / mitmproxy SSL bump / step-ca 発行 等）。
3. 起動時 RSS を実測し、リソース見積（[実装可能性マトリクス](../02_基本設計/実装可能性マトリクス.md)）を更新する。
4. Low / ブロッカーは代替差し替えルールを適用し、差し替え後の見込みを記録する。
5. 結果を `03_詳細設計/軽量検証結果_2026-07-04.md` に記録し、マトリクスの確度列を確定する。

## 合格の考え方

- 各観点で「arm64 で成立する実装候補が1つ以上ある」ことを合格とする（[要件定義書](../01_要件定義/要件定義書.md) AC-検証）。
- 全観点が代替を含めて成立すれば、Phase 1-6 の本格実装（後続テーマ）に進む見込みが立ったと判断する。

## 参照

- [実装可能性マトリクス](../02_基本設計/実装可能性マトリクス.md)
- [段階ロードマップ](../02_基本設計/段階ロードマップ.md)
- [要件定義書](../01_要件定義/要件定義書.md)
