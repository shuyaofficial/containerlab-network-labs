---
type: troubleshoot
theme: "ゼロトラスト_ネットワーク特化"
status: draft
date: 2026-07-08
tags: [troubleshoot, "ゼロトラスト_ネットワーク特化", nac, ztna, ndr, microsegmentation]
title: "切り分けシート — ゼロトラスト_ネットワーク特化 (原本)"
---

# 切り分けシート: ゼロトラスト_ネットワーク特化

> **本ファイルは原本（テンプレート）である。障害発生のたびに複製して使用し、原本には直接書き込まない。**
> 複製先ファイル名は `切り分けシート_YYYY-MM-DD.md` とする。
> 過去の教訓（Obsidian Mistakes.md）に基づき、以下の順序を必ず守ること:
> 1. ライブ状態を先に採取する（設定ファイルを読む前に、まず実機の今の状態を見る）
> 2. 層別（L2→VLAN→SVI→L3→アプリ/overlay）に切り分ける
> 3. 単一原因を決め打ちせず、複数の独立原因を並行して疑う

## 1. 症状記録

- 発生日時:
- 実行したコマンド / 操作:
- 期待した結果:
- 実際の結果:
- 再現性: {{毎回 / 断続的 / 1回のみ}}

## 2. ライブ状態採取（設定解析より先に行う）

<!-- 鉄則: core_sw_merged.cfg・nwzt.clab.yml等の設定ファイルを読みに行く前に、
     まず実機のshowコマンド・docker execで「今何が起きているか」を採取する。 -->

| コマンド | 実行ノード | 出力の要約 |
|---|---|---|
| `show authentication sessions` | core-sw | |
| `show dot1x all summary` | core-sw | |
| `show ip access-lists SEG10-OUT` | core-sw | |
| `show vlan brief` | core-sw | |
| `show ip interface brief \| include Vlan` | core-sw | |
| `docker ps --format 'table {{.Names}}\t{{.Status}}'` | VM | |
| `./deploy-all.sh inspect` | VM(04_構築) | |
| `./deploy-all.sh eve` | VM(04_構築) | |

## 3. 層別切り分け表

<!-- L1/L2到達 → VLAN存在 → SVI Up → L3(ACL)到達 → NDR検知 → ZTNA overlay到達 の順で埋める。
     上位層の異常は下位層の異常の結果であることが多いため、必ず下から確認する。 -->

| 層 | 確認項目 | コマンド | 期待 | 実測 | 判定 |
|---|---|---|---|---|---|
| L1/L2 | データプレーン疎通（iouyap） | `sudo docker exec clab-nwzt-lan-core-sw ps aux \| grep iouyap` | プロセス起動中 | | ⬜ |
| L2 | 802.1X認証状態 | `show authentication sessions` | pc-sales=AUTHORIZED/VLAN10、pc-unauth=VLAN99 | | ⬜ |
| L2 | VLAN存在 | `show vlan brief` | 10/20/30/50/99/100が存在 | | ⬜ |
| L3 | SVI Up | `show ip interface brief \| include Vlan` | 全SVIがup/up | | ⬜ |
| L3 | ACL適用 | `show ip access-lists SEG10-OUT` | tcp80 permitカウンタが増加、tcp22 denyカウンタが増加 | | ⬜ |
| L3/L4 | inter-VLAN疎通 | `docker exec clab-nwzt-lan-pc-sales curl -s -o /dev/null -w '%{http_code}' http://172.31.50.11/` | 200 | | ⬜ |
| N4層2 | ホストnftables | `docker exec clab-nwzt-lan-srv-app1 nft list ruleset` | host-infected(172.31.50.31)からのdropルールが存在 | | ⬜ |
| N3 | NDR検知 | `./deploy-all.sh eve` | sid:1000001のalertが記録 | | ⬜ |
| N2 | ZTNA overlay到達 | `docker exec clienttun curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/` | 200 | | ⬜ |

## 4. 複数原因の並行仮説

<!-- 同一症状に独立した原因が重複する前提で、最低2つの仮説を立てる。
     1つの仮説で説明がついても、他の仮説を消去法で潰すまでは断定しない。 -->

### 仮説A:

- 根拠:
- 検証方法:
- 結果: ⬜ 確認済み / ⬜ 否定済み / ⬜ 未検証

### 仮説B:

- 根拠:
- 検証方法:
- 結果: ⬜ 確認済み / ⬜ 否定済み / ⬜ 未検証

<!-- 必要であれば仮説Cを追加する（例: iouyap未起動とサーバ室ブリッジ未作成が同時に起きていたケース等） -->

## 5. 根本原因・恒久対策

- 根本原因:
- 恒久対策:
- ドキュメントへの反映（該当する場合、更新したファイルを列挙）:

## 6. AAR（①予想 ②実際 ③次どうする）

- ① 予想:
- ② 実際:
- ③ 次どうする:

## 参照

- [05_試験/試験計画書.md](試験計画書.md)
- [03_詳細設計/パラメータシート.md](../03_詳細設計/パラメータシート.md)
- [03_詳細設計/CLI投入コマンド.md](../03_詳細設計/CLI投入コマンド.md)
- [04_構築/deploy-all.sh](../04_構築/deploy-all.sh)
- [規約/clab運用規約.md](../../規約/clab運用規約.md)
