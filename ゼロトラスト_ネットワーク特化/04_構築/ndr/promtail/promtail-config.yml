# Promtail: Suricata の eve.json を tail し、job="suricata" ラベルで Loki へ送る。
# host mode で起動するため Loki は localhost:3100。
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/promtail-positions.yaml

clients:
  - url: http://localhost:3100/loki/api/v1/push

scrape_configs:
  - job_name: suricata
    static_configs:
      - targets:
          - localhost
        labels:
          job: suricata
          host: ndr-sensor
          __path__: /var/log/suricata/eve.json
    pipeline_stages:
      # eve.json は 1 行 1 JSON。event_type をラベル化して LogQL で絞れるようにする。
      - json:
          expressions:
            event_type: event_type
      - labels:
          event_type:
