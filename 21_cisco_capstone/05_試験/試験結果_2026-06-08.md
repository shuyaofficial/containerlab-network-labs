# 試験結果（疎通・正常性確認）

- **日付**: 2026-06-08
- **対象**: capstone ラボ（全6台 Cisco IOL + PC 2台）
- **総合判定**: ✅ **全項目 PASS**
- **関連**: 構築の経緯・根本原因は `04_構築/構築ログ_2026-06-08.md` を参照。

---

## 0. 試験項目サマリ

| # | 試験項目 | 期待値 | 結果 |
| :-- | :--- | :--- | :--: |
| T1 | 全6台の管理SSH疎通 | 6台すべてSSH可（core含む） | ✅ PASS |
| T2 | OSPF 隣接 | hq-core1 で3隣接が `FULL` | ✅ PASS |
| T3 | HSRP（VLAN10） | core1=Active(150) / core2=Standby(100), VIP 10.1.10.254 | ✅ PASS |
| T4 | EtherChannel（コア間） | `Po1(SU)` LACP, Et1/1・Et1/2 bundled | ✅ PASS |
| T5 | eBGP（対ISP） | edge1/edge2 とも `Established`、デフォルト受信 | ✅ PASS |
| T6 | デフォルトルート伝播 | edge=BGP由来 / core=OSPF E2 | ✅ PASS |
| T7 | 本社PC→インターネット | `8.8.8.8` へ ping 0% loss（HSRP-GW経由） | ✅ PASS |
| T8 | 支社PC→インターネット | `8.8.8.8` へ ping 0% loss（br-edge経由） | ✅ PASS |
| T9 | NAT（PAT）変換 | edge1/br-edge で変換エントリ生成 | ✅ PASS |

---

## T1. 管理SSH疎通（全6台）

| 機器 | 管理IP | 結果 |
| :--- | :--- | :--: |
| isp | 172.20.20.11 | ✅ `hostname isp` |
| hq-edge1 | 172.20.20.12 | ✅ `hostname hq-edge1` |
| hq-edge2 | 172.20.20.13 | ✅ `hostname hq-edge2` |
| br-edge | 172.20.20.14 | ✅ `hostname br-edge` |
| hq-core1 | 172.20.20.21 | ✅ `hostname hq-core1` |
| hq-core2 | 172.20.20.22 | ✅ `hostname hq-core2` |

---

## T2. OSPF 隣接（hq-core1）

```
hq-core1#show ip ospf neighbor

Neighbor ID     Pri   State           Dead Time   Address         Interface
4.4.4.4           1   FULL/BDR        00:00:37    10.1.10.253     Vlan10
2.2.2.2           1   FULL/BDR        00:00:30    10.0.0.9        Ethernet0/2
1.1.1.1           1   FULL/BDR        00:00:38    10.0.0.1        Ethernet0/1
```
→ core2(4.4.4.4) / edge2(2.2.2.2) / edge1(1.1.1.1) と **3隣接すべて FULL**。

---

## T3. HSRP（VLAN10）

```
hq-core1#show standby brief
Interface   Grp  Pri P State   Active          Standby         Virtual IP
Vl10        10   150 P Active  local           10.1.10.253     10.1.10.254

hq-core2#show standby brief
Interface   Grp  Pri P State   Active          Standby         Virtual IP
Vl10        10   100 P Standby 10.1.10.252     local           10.1.10.254
```
→ **core1=Active(pri150) / core2=Standby(pri100)**、仮想IP `10.1.10.254`、preempt 有効。設計どおり。

---

## T4. EtherChannel（コア間 Po1 / LACP）

```
hq-core1#show etherchannel summary
Number of channel-groups in use: 1
Number of aggregators:           1

Group  Port-channel  Protocol    Ports
------+-------------+-----------+-----------------------------------------------
1      Po1(SU)         LACP      Et1/1(P)    Et1/2(P)
```
→ `Po1(SU)`（S=Layer2, U=in use）、Et1/1・Et1/2 とも `(P)=bundled`。LACP 正常。

---

## T5. eBGP（対 ISP）

```
hq-edge1#show ip bgp summary | begin Neighbor
Neighbor        V           AS MsgRcvd MsgSent   TblVer  InQ OutQ Up/Down  State/PfxRcd
200.0.1.254     4        65000      21      20        2    0    0 00:15:42        1

hq-edge2#show ip bgp summary | begin Neighbor
Neighbor        V           AS MsgRcvd MsgSent   TblVer  InQ OutQ Up/Down  State/PfxRcd
200.0.2.254     4        65000      21      20        2    0    0 00:15:33        1
```
→ edge1/edge2 とも ISP(AS65000) と Established、`PfxRcd=1`（デフォルトルート受信）。Up時間も安定。

---

## T6. デフォルトルート伝播

```
hq-edge1#show ip route 0.0.0.0
Routing entry for 0.0.0.0/0, supernet
  Known via "bgp 65001", distance 20, metric 0, candidate default path
  * 200.0.1.254, from 200.0.1.254 ...

hq-core1#show ip route 0.0.0.0
Routing entry for 0.0.0.0/0, supernet
  Known via "ospf 1", distance 110, metric 1, candidate default path
  Tag 1, type extern 2, forward metric 10
  * 10.0.0.9, from 2.2.2.2, via Ethernet0/2
    10.0.0.1, from 1.1.1.1, via Ethernet0/1
```
→ edge は BGP 由来、core は OSPF **E2** で学習（edge1/edge2 両方経由＝ECMP）。
（補足: IOSの `show ip route 8.8.8.8`（ホスト指定）は仕様上デフォルト経路を表示せず `% Network not in table` を返すが、`show ip route 0.0.0.0` のとおりデフォルト経路は導入済み。実際の疎通は T7/T8 で確認。）

---

## T7. 本社PC（HQ-PC-Sales 10.1.10.100）→ インターネット

```
hq-pc-sales:~# ping -c 3 10.1.10.254   # HSRP 仮想GW
3 packets transmitted, 3 received, 0% packet loss

hq-pc-sales:~# ping -c 3 8.8.8.8         # NAT 越しインターネット
64 bytes from 8.8.8.8: icmp_seq=3 ttl=253 time=4.55 ms
3 packets transmitted, 3 received, 0% packet loss
```
→ GW（HSRP-VIP）・インターネットとも **0% loss**。経路: PC → core(HSRP) → OSPFデフォルト → edge → NAT → ISP(8.8.8.8)。

---

## T8. 支社PC（BR-PC 10.2.40.100）→ インターネット

```
br-pc:~# ping -c 3 10.2.40.254          # 支社GW(br-edge)
3 packets transmitted, 3 received, 0% packet loss

br-pc:~# ping -c 3 8.8.8.8                # NAT 越しインターネット
64 bytes from 8.8.8.8: icmp_seq=3 ttl=254 time=2.29 ms
3 packets transmitted, 3 received, 0% packet loss
```
→ GW・インターネットとも **0% loss**。経路: PC → br-edge → 静的デフォルト → NAT → ISP(8.8.8.8)。

---

## T9. NAT（PAT）変換テーブル

```
hq-edge1#show ip nat translations
Pro Inside global      Inside local       Outside local      Outside global
icmp 200.0.1.1:4140    10.1.10.100:4140   8.8.8.8:4140       8.8.8.8:4140

br-edge#show ip nat translations
Pro Inside global      Inside local       Outside local      Outside global
icmp 200.0.3.1:35446   10.2.40.100:35446  8.8.8.8:35446      8.8.8.8:35446
```
→ 本社: `10.1.10.100 → 200.0.1.1`、支社: `10.2.40.100 → 200.0.3.1` で **PAT 変換が実機動作**していることを確認。

---

## 参考: 採取コマンド（clab マシン内で実行）

```bash
SSH_OPTS="-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=10 \
-o KexAlgorithms=+diffie-hellman-group14-sha1 -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa"
run() { printf "terminal length 0\n%b\nexit\n" "$2" | sshpass -p admin ssh $SSH_OPTS admin@$1; }
# 例: run 172.20.20.21 "show ip ospf neighbor\nshow standby brief\nshow etherchannel summary"
# PC: sudo docker exec clab-capstone-hq-pc-sales ping -c 3 8.8.8.8
```
※ IOS への show 投入は `;` 区切りでは動かないため、**標準入力にコマンドを流し込む**方式を使う。
