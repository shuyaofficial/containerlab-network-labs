# 🎓 第7章：統合キャプストン（卒業制作）

お疲れ様でした！いよいよネットワーク技術の総決算となる「統合キャプストン」に挑戦します。
これまでに学んだすべての知識を駆使し、**要件定義書に沿って、ゼロから本社・支社ネットワークを構築**してください。

---

## 🏗️ ネットワーク構成とシナリオ
あなたは、中堅企業（社員120名）のインフラエンジニアです。
今回、**「絶対に止まらない本社ネットワーク」** と **「本社へ安全に繋がる支社ネットワーク」** の構築を任されました。

構成図（論理イメージ）：
```text
      [インターネット (ISP)]
        /          |         \
  [HQ-Edge1]  [HQ-Edge2]    [BR-Edge] (支社ルーター)
      \           /             |
    [HQ-Core1]===[HQ-Core2]    [BR-PC] (支社PC)
        |
   [HQ-PC-Sales] (本社PC)
```

---

## 🛠️ フェーズ1：要件とパラメータの確認

### 1. IPアドレス設計（要件）
| ノード/インターフェース | 用途 | IPアドレス | 備考 |
|---|---|---|---|
| **ISP** (e0/1) | 本社向けメイン回線 | `200.0.1.254/24` | グローバルIP相当 |
| **ISP** (e0/2) | 本社向けサブ回線 | `200.0.2.254/24` | グローバルIP相当 |
| **ISP** (e0/3) | 支社向け回線 | `200.0.3.254/24` | グローバルIP相当 |
| **HQ-Edge1** (e0/1) | ISPメイン接続 | `200.0.1.1/24` | NAT(PAT)変換の出口 |
| **HQ-Edge2** (e0/1) | ISPサブ接続 | `200.0.2.1/24` | NAT(PAT)変換の出口 |
| **BR-Edge** (e0/1) | ISP支社接続 | `200.0.3.1/24` | NAT(PAT)変換の出口 |
| **本社 コア間** | 内部OSPF用リンク | `10.0.0.0/24` 帯で任意 | Edge1/2, Core1/2 を接続 |
| **HQ-Core (VLAN 10)** | 営業部用セグメント | `10.1.10.0/24` | HSRP仮想IP: `10.1.10.254` |
| **HQ-PC-Sales** | 営業部PC | `10.1.10.100/24` | GW: `10.1.10.254` |
| **BR-Edge** (e0/2) | 支社内LAN | `10.2.40.254/24` | |
| **BR-PC** | 支社PC | `10.2.40.100/24` | GW: `10.2.40.254` |

---

## 🚀 フェーズ2：構築ミッション

以下のミッションを、**あなたがこれまで培った知識で、自分の好きな順番で** 構築してください。
（わからなくなった時は、過去のフォルダのREADMEやコマンド履歴を見返して構いません！）

### ミッション①：本社の冗長化LAN構築（L2/L3・OSPF・HSRP）
1. `HQ-Core1` と `HQ-Core2` の間に VLAN 10 を作成し、PCを接続する。
2. コアスイッチ間のリンクを EtherChannel（トランク）で冗長化する。
3. `HQ-Core1` を STP のルートブリッジ、かつ HSRP の Active（仮想IP: `10.1.10.254`）に設定する。
4. `HQ-Edge1`, `HQ-Edge2`, `HQ-Core1`, `HQ-Core2` の間で **OSPF (Area 0)** を構成し、社内のルーティングを確立する。

### ミッション②：インターネット境界とBGPデュアルホーム
1. `HQ-Edge1` と `HQ-Edge2` でインターネットに出るための **NAT (PAT)** を設定する。（内部の10.x.x.xを、外部インターフェースのIPに変換）
2. ISPとの間で **BGP** を確立する。（ISPのAS番号を `65000`、本社のAS番号を `65001` とする）
3. BGPの Local Preference 等を操作し、通常時は `HQ-Edge1`（メイン回線）を通ってインターネットに出るようにする。

### ミッション③：拠点間 IPsec VPN
1. 本社（`HQ-Edge1`）と 支社（`BR-Edge`）の間にインターネット越し（200.x.x.x）の **IPsec VPN** を構築する。
2. 対象となる通信は「支社(10.2.40.0/24) ⇔ 本社営業部(10.1.10.0/24)」のみとする。

---

## 🎯 フェーズ3：最終試験（テスト要件）

すべての構築が終わったら、以下のテストを実施し、すべてクリアできれば**合格（卒業）**です！

✅ **テスト1：インターネット疎通**
`HQ-PC-Sales` および `BR-PC` から、ISP（`200.0.1.254` など）に Ping が届くこと。（NATが成功している証拠）

✅ **テスト2：拠点間通信の暗号化**
`BR-PC` から `HQ-PC-Sales (10.1.10.100)` に Ping が届くこと。かつ、その通信がインターネット上（ISPのルーター）では暗号化されていて中身が見えないこと。

✅ **テスト3：究極の耐障害性テスト**
`HQ-PC-Sales` から ISP へ Ping を打ち続けている状態で、**`HQ-Edge1` のコンソールから e0/1 を shutdown する。**
数秒の断のあと、自動的にサブ回線（HQ-Edge2）へ経路が切り替わり、Pingが復活すること。

---

## 🌟 はじめかた
まずは環境をデプロイし、機器にログインしてコンフィグを流し込み始めましょう！
```bash
cd /Users/shuya/Documents/claude/Mac仮想環境構築/21_cisco_capstone
sudo containerlab deploy -t capstone.clab.yml
```
健闘を祈ります！！
