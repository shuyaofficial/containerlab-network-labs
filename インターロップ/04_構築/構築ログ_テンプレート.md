# 構築ログテンプレート

日付:
担当:

## 0. Theme 22保護確認

- [ ] `sudo containerlab inspect --all` で `campus` の状態を確認した。
- [ ] `interop-shownet` 以外をdeploy/destroyしないことを確認した。
- [ ] 作業ディレクトリが `Mac仮想環境構築/インターロップ` であることを確認した。

## 1. デプロイ

```bash
ssh clab@orb
cd /Users/shuya/Documents/claude/Mac仮想環境構築/インターロップ
./tools/deploy_interop.sh
```

結果:

- [ ] 全ノードがrunning。
- [ ] Cisco IOLノードで `iouyap` が起動。
- [ ] LinuxノードでIP/デフォルトルートが設定済み。

## 2. ルーティング確認

```text
show ip ospf neighbor
show ip route ospf
show ip bgp summary
show ip bgp
```

結果:

- [ ] OSPF隣接が期待通り。
- [ ] edge-n1/ext-bbix eBGPがEstablished。
- [ ] edge-n2/ext-jpix eBGPがEstablished。
- [ ] edge-n1/edge-n2 iBGPがEstablished。

## 3. Media over IP風テスト

```bash
docker exec -it clab-interop-shownet-media-x ping 10.90.41.10
docker exec -it clab-interop-shownet-media-x ping 10.90.42.10
```

結果:

- [ ] `media-x` から `yokohama-foh` へ疎通。
- [ ] `media-x` から `hamamatsu-mocap` へ疎通。

## 4. セキュリティ確認

```text
remote-s5# ping 10.90.60.10 source Loopback0
remote-s5# ping 10.90.40.10 source Loopback0
sec-s4# show access-lists REMOTE-S5-IN
```

結果:

- [ ] 監視系は許可。
- [ ] Media系は拒否カウンタが増える。

## 5. 振り返り

- 予想:
- 実際:
- 次どうする:
