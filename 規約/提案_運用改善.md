---
type: proposal
theme: "規約"
status: review
date: 2026-07-03
tags: [proposal, operations, git, image-manifest, aar]
title: "運用改善提案書 — Containerlabラボ環境"
---

# 運用改善提案書 — Containerlabラボ環境

> 対象: `Mac仮想環境構築/` 配下の全テーマ運用。**既存テーマ02〜27には遡及しない**（[README.md](README.md) の適用範囲どおり）。
> 本書は「あるべき運用」を提案として記録するものであり、指摘のための指摘ではない。次にそのテーマへ手を入れる時の判断材料とする。

---

## B-1. VM/イメージ再構築性

### 課題

2026-06-15にOrbStack VM「clab」が消失し、ビルド済みイメージ18個を失った。復旧に3日を要した。原因は「頭の中にしかない再構築手順」と「イメージの現状を一覧できる場所がなかったこと」の二つ。

### 提案

| 施策 | 内容 | 状態 |
|---|---|---|
| イメージマニフェストの常設 | `環境/イメージマニフェスト.md` に稼働中の全イメージ（タグ・kind・アーキテクチャ・ビルド日・素材の入手経路）を一覧化 | 別タスクで実データ生成中（本書と並行） |
| 再構築runbookの常設 | [rebuild_runbook.md](../環境/rebuild_runbook.md) をVM再構築の単一手順書として維持 | 本タスクで新規作成 |
| 四半期arm64棚卸し | `docker manifest inspect <image>` で各イメージのプラットフォーム（linux/amd64 か linux/arm64 か）を確認し、arm64ネイティブ版が新規に出ていないか確認する。QEMUエミュ実行しているイメージ（IOL・FortiGate等）は起動が遅く、ネイティブ代替（cEOS・SR Linux・cRPD等）が出ていれば移行を検討 | 未着手・四半期毎（次回目安: 2026-10） |

### 運用ルール

- イメージマニフェストとrunbookは**セットで更新する**。イメージを追加・変更したらマニフェストに追記し、再構築手順に影響があればrunbookも直す。
- VM消失は「起きるもの」として扱う。マニフェスト＋runbookの2点があれば、次回消失時の復旧は「素材の再DL待ち」のみに縮退できる（今回の教訓）。

---

## B-2. Git/GitHub公開手順

### 課題

`Mac仮想環境構築/` は現状**未git管理**（132MB）。公開判断は個別テーマ単位で発生しうるが、以下がリポジトリ直下に混在している。

- 実行時生成物: `clab-*/` ランタイムディレクトリ（**TLS CA秘密鍵を含む**。2026-07-03時点で12箇所の `clab-*/.tls/ca/ca.key` を確認済み）
- 大容量バイナリ: Ciscoの `.bin`（IOL）・`.qcow2`（FortiGate等）
- ライセンス関連: `IOURC*` およびその展開物
- 生成された構成図・ビルド出力（`規約/ビルド/` のHTML出力想定）

これらを未サニタイズのままpushすると、秘密鍵の漏洩・ライセンス条項違反・リポジトリ肥大化のいずれかが起きる。

### 提案: `.gitignore` 方針

| パターン | 理由 |
|---|---|
| `clab-*/` | TLS CA秘密鍵・実行時状態を含むランタイムディレクトリ全体を除外 |
| `*.bin` | Cisco IOLバイナリ（配布不可のライセンス素材） |
| `*.qcow2` | FortiGate等のVMイメージ本体 |
| `IOURC*` | Cisco IOLライセンスファイルおよびzip展開物 |
| `*license*` | ライセンス関連ファイルの汎用除外（大文字小文字問わず運用） |
| `node_modules/` | ビルドツール（`規約/ビルド/build.mjs`）の依存物 |
| `docs/assets/` などビルド生成物 | MD→HTMLビルドの出力物（再生成可能なため） |

### 提案: サニタイズ工程（公開前の走査手順）

1. **秘密鍵走査**: `find . -path "*clab-*/.tls*"` で残存確認 → 上記gitignoreが効いていれば0件のはず。念のため `git status --ignored` で除外漏れがないか確認。
2. **ライセンス文字列走査**: `grep -ril "iourc\|hostid\|license" --include='*.md' --include='*.yml'` でライセンス関連の記述を洗い出し、実際のキー・シリアル値が平文で書かれていないか確認（手順書としての言及は可、実値の埋め込みは不可）。
3. **個人情報・認証情報走査**: `grep -rilE "password|passwd|secret|token" --include='*.md' --include='*.yml' --include='*.sh'` でハードコード認証情報を確認。ラボ環境の初期パスワード（例: FortiGateの初回未設定パスワード）は「未設定である旨の説明」であればドキュメント上問題ないが、実際に設定した本番相当の値は除外する。
4. **ユーザー名・パス走査**: `grep -r "/Users/shuya"` で絶対パスの露出を確認し、相対パス・プレースホルダに置換する。

### 公開前チェックリスト

- [ ] `.gitignore` を上記方針で設置済み
- [ ] `git status --ignored` で `clab-*/` ・バイナリ・ライセンス類が除外リストに乗っている
- [ ] 上記3種のgrep走査を実施し、ヒットをレビュー済み
- [ ] 絶対パス（`/Users/shuya/...`）を相対パスまたはプレースホルダに置換済み
- [ ] READMEやCLAUDE.mdに含まれる内部限定情報（社内ネットワーク構成の実IP帯など）を確認済み

---

## B-3. 試験・切り分け教訓のテンプレ化

### 課題

障害切り分けは各テーマで都度その場で記録しており、様式が揺れている（例: `試験結果_YYYY-MM-DD.md` と `試験完了記録_YYYY-MM-DD.md` の混在、`構築ログ_テンプレート.md` の有無がテーマにより異なる）。過去の運用教訓（Obsidian `Mistakes.md`）として蓄積されている以下の5点は、個別テーマの記録に埋もれると再利用されにくい。

1. 外部リソースは手持ちストレージ網羅検索が先
2. ライブ状態（show系コマンド）を設定解析より先に採取
3. 同一症状に複数独立原因を想定し層別切り分け
4. 日本語ファイル名はNFC正規化
5. iCloudロックは `brctl evict`/`brctl download` で回避

### 提案

- `05_試験/切り分けシート.md` の雛形（`type: troubleshoot`）を [規約/雛形/](雛形/) に追加し、新規テーマは必ずこの雛形からコピーする。
- 雛形の冒頭に上記5教訓を「着手前チェック」として埋め込み、都度Obsidianを参照しなくても切り分け時に自然に思い出せる形にする。
- 個別テーマの切り分けシートで新たな教訓が見つかった場合、そのテーマ内に留めず、意味のある区切りでObsidian `Mistakes.md` へも追記する（グローバル運用ルールに準拠）。テンプレへの反映は四半期棚卸し（B-1）のタイミングでまとめて検討する。

---

## B-4. 進捗・AAR接続

### 課題

進捗の状態は3箇所に分散している。

1. 各テーマMDの frontmatter `status`（draft/review/done/superseded）
2. [ロードマップ/進捗管理.md](../ロードマップ/進捗管理.md) の状態凡例（✅/◐/⏹/⚠/⬜）
3. Obsidian長期記憶（`Daily/`・`Decisions.md`・`Mistakes.md`）のAAR記録

これらは現状、更新のタイミングも粒度も独立しており、片方を更新してももう片方が追随しない（例: テーマ22は `⏹部分達成で終了` だが、フォルダ内の個別MDの `status` は未統一）。

### 提案: 双方向リンク運用

| 更新元 | 更新すべき先 |
|---|---|
| テーマ内MDの `status` を `done` に変更 | [進捗管理.md](../ロードマップ/進捗管理.md) の該当行の状態記号を更新し、可能なら振り返りファイルへのリンクを追加 |
| [進捗管理.md](../ロードマップ/進捗管理.md) の状態を更新 | 該当テーマの主要MD（README・基本設計書など）の frontmatter `status` も揃える |
| 意味のある区切り／タスク完了 | Obsidian `Daily/YYYY-MM-DD.md` に該当テーマ名を明記して追記（グローバル運用ルールに準拠、本フォルダ内には書かない） |
| AARで「予想と実際のズレ」が学びだった場合 | Obsidian `Mistakes.md` へ。テーマ固有の切り分けシートにも該当があれば併記 |

- 3箇所の同期は自動化しない（過剰な仕組み化を避ける）。「テーマの状態が変わったら3箇所を思い出して見る」をセルフ監査チェックリストの項目として明文化するに留める。

---

## B-5. 既存テーマの不整合一覧（記録のみ・遡及修正しない）

以下は2026-07-03時点で確認した既存テーマ02〜27の不整合。**規約は新規テーマ（28以降）にのみ適用**するため、ここでは是正せず記録に留める。次にそのテーマへ手を入れる機会があれば、規約へ寄せることを推奨する。

| # | 不整合 | 該当テーマ | 推奨対応（次回着手時） |
|---|---|---|---|
| 1 | `clab.yml` 配置がテーマ直下（02〜22）と `04_構築/`配下（23〜27）で混在 | 02〜27全般 | [clab運用規約.md](clab運用規約.md) の配置ルールに合わせて `04_構築/` 配下へ寄せる |
| 2 | `deploy.sh`（22のみ `build_and_deploy.sh`）がテーマ毎に非標準 | 22（独自名）、23〜27（`deploy.sh`） | スクリプト名・引数体系（status/build/deploy/destroy）を統一形に揃える |
| 3 | 構成図の形式がdrawio（22のみ）／テキスト版／mermaid混在 | 22（drawio）、他（mermaid or テキスト） | [構成図規約.md](構成図規約.md) のMermaid第一方針に揃え、drawioは非推奨として維持のみ |
| 4 | テーマ番号「05」が2フォルダで重複（`05_cisco_bgp_ospf_redistribute` と `05_cisco_bgp_route_map_rr`） | 05番台 | 番号の付け直しは既存リンク破壊のリスクが高いため据え置き。新規テーマでは重複を出さないことを [ドキュメント標準.md](ドキュメント標準.md) で担保 |
| 5 | 試験結果ファイル名が `試験結果_YYYY-MM-DD.md` と `試験完了記録_YYYY-MM-DD.md` で揺れ | 21・22（試験結果）、24（試験完了記録） | frontmatterスキーマの `test-result` type に統一し、ファイル名も `試験結果_YYYY-MM-DD.md` に寄せる |
| 6 | `構築ログ_テンプレート.md` の設置がテーマにより有無が分かれる（24〜27・インターロップにはあるが22以前にはない） | 22以前 | 新規着手時に雛形からコピーする運用に統一（本タスクの対象外、雛形自体は既に存在） |
| 7 | frontmatter未整備の可能性（本規約は2026-07-03制定のため、それ以前に書かれたMDは値辞書に沿っていない場合がある） | 02〜27全般 | 各テーマに触る際、対象MDのfrontmatterを [frontmatterスキーマ.md](frontmatterスキーマ.md) に合わせて補完 |

### 棚卸しの位置づけ

- 上表は一括修正のタスクリストではない。**「次にそのテーマを触る理由ができた時」に合わせて是正する**という前提の記録。
- 新規で棚卸し項目を見つけた場合は、この表に行を追加する（表自体をこの提案書の唯一の記録先とし、二重管理しない）。

---

## 改訂ルール

本書の変更は [README.md](README.md) の改訂ルールに従う。既存テーマへの遡及修正は行わず、移行推奨として本書に追記する。
