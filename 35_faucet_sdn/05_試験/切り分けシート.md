---
type: troubleshoot
theme: "35_faucet_sdn"
status: draft
date: 2026-07-07
tags: [sdn, openflow, faucet, troubleshoot]
title: "切り分けシート — Faucet SDN"
---

# 切り分けシート: Faucet SDN

<!-- 障害発生のたびに本ファイルを複製して使う（ファイル名に日付を付ける運用: 切り分けシート_YYYY-MM-DD.md）。
     過去の教訓（Obsidian Mistakes.md）に基づき、以下の順序を必ず守ること:
     1. ライブ状態を先に採取する（設定ファイルを読む前に、まず実機の今の状態を見る）
     2. 層別（L2→VLAN→OpenFlow制御チャネル→L3）に切り分ける
     3. 単一原因を決め打ちせず、複数の独立原因を並行して疑う -->

## 1. 症状記録

<!-- いつ・何をしたら・何が起きたか。再現手順があれば書く。推測は書かない -->
- 発生日時:
- 実行したコマンド / 操作:
- 期待した結果:
- 実際の結果:
- 再現性: {{毎回 / 断続的 / 1回のみ}}

## 2. ライブ状態採取（設定解析より先に行う）

<!-- 鉄則: faucet.yaml やbootstrap.shを読みに行く前に、まず実機のshow系コマンドで
     「今何が起きているか」を採取する。設定を先に読むと「こう書いたはずだから動くはず」
     という思い込みで見落としが起きる。 -->

| コマンド | 実行ノード | 出力の要約 |
|---|---|---|
| `ovs-vsctl show` | ovs1 | |
| `ovs-ofctl show br0` | ovs1 | |
| `ovs-ofctl dump-flows br0` | ovs1 | |
| `docker logs --tail 100 clab-faucet-sdn-faucet` | faucet | |
| `docker ps` / `containerlab inspect` | VM | |

## 3. 層別切り分け表

<!-- L1/L2到達 → OVSポート収容 → OpenFlow制御チャネル → VLANポリシー反映 → L3到達 の順で埋める。
     上位層の異常は下位層の異常の結果であることが多いため、必ず下から確認する。 -->

| 層 | 確認項目 | コマンド | 期待 | 実測 | 判定 |
|---|---|---|---|---|---|
| L1/L2 | veth収容・リンクUP | `ovs-vsctl show` / `ip link show` (ovs1内) | 各eth interfaceがbr0に所属しUP | | ⬜ |
| L2 | OVSポートのofport番号 | `ovs-ofctl show br0` | パラメータシート通りのofport | | ⬜ |
| 制御チャネル | OpenFlow接続 | `ovs-vsctl show` の Controller | `is_connected: true` | | ⬜ |
| 制御チャネル | Faucet側ロード状況 | `docker logs` (faucet) | `faucet.yaml` ロード成功・エラーなし | | ⬜ |
| フロー | VLANポリシー反映 | `ovs-ofctl dump-flows br0` | 意図したVLANでのマッチが存在 | | ⬜ |
| L3/E2E | エンドツーエンド疎通 | `ping` (pc-a/b/c間) | Mission要件どおりの成功/失敗 | | ⬜ |

## 4. 複数原因の並行仮説

<!-- 同一症状に独立した原因が重複する前提で、最低2つの仮説を立てる。
     1つの仮説で説明がついても、他の仮説を消去法で潰すまでは断定しない。 -->

### 仮説A:

- 根拠:
- 検証方法:
- 結果: ⬜ 確認済み / ⬜ 否定済み / ⬜ 未検証

### 仮説B:

- 根拠:
- 検証方法:
- 結果: ⬜ 確認済み / ⬜ 否定済み / ⬜ 未検証

<!-- 必要であれば仮説Cを追加する -->

## 5. 根本原因・恒久対策

<!-- 仮説の検証結果から特定した根本原因と、再発防止のための恒久対策を書く -->
- 根本原因:
- 恒久対策:
- ドキュメントへの反映（該当する場合、更新したファイルを列挙）:

## 6. AAR（①予想 ②実際 ③次どうする）

<!-- グローバル運用ルールの仮説→振り返りループに準拠。着手前の予想と実際のズレを起点に学ぶ -->
- ① 予想:
- ② 実際:
- ③ 次どうする:
