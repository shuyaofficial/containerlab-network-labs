# 構築ログ — Theme 24: Routed Access環境起動

## 1. 実施内容

- 実施日: 2026-06-30
- 実行場所: OrbStack Linux VM `clab`
- 対象: `24_campus_routed_access/04_構築/campus.clab.yml`
- 実施範囲: ノード・リンク・管理ネットワークの作成のみ
- 未実施: IOSデータプレーン、OSPF、Loopback、端末IP、静的経路

## 2. 起動結果

| 項目 | 結果 |
|---|---|
| containerlabバージョン | 0.77.0 |
| ラボ名 | `campus-routed-access` |
| Cisco IOL | core1、core2、access1、access2の4台がrunning |
| Linux端末 | pc-a、pc-bの2台がrunning |
| 仮想リンク | 7本作成成功 |
| 管理ネットワーク | `campus-routed-access-mgmt` / `172.24.24.0/24` |
| IOL起動 | 4台で`qemu-i386 /iol/iol.bin`をPID1として確認 |
| iouyap | deploy後に4台で`iouyap 513`を起動 |
| 初期状態 | Cisco System Configuration Dialogで待機 |
| attach検証 | core1/core2/access1/access2の4台でEnter入力と初期設定プロンプトを確認 |

## 3. 未設定確認

- `campus.clab.yml`に`startup-config`なし。
- `campus.clab.yml`に`exec`なし。
- `entrypoint_attach.sh`はIOLをPID1で起動するだけで、IOSコンフィグを投入しない。
- pc-aの`eth1`にIPv4アドレスなし、データプレーン経路なし。
- pc-bの`eth1`にIPv4アドレスなし、データプレーン経路なし。
- OSPF、Loopback、インフラIP、端末IPは未投入。

## 4. 初回操作

### 実行場所: OrbStack Linux VM `clab`

```bash
sudo docker attach --sig-proxy=false clab-campus-routed-access-core1
```

- 初期セットアップダイアログへの回答は学習者が選択する。
- IOS設定コマンドは本ログに記載しない。
- コンテナを停止せず離脱する場合は`Ctrl-p`、続けて`Ctrl-q`。

## 5. AAR

- 予想: 6ノード・7リンクを起動し、データプレーン未設定で引き渡せる。
- 実際: 全ノード・リンクは起動し、端末データプレーンは空。Theme 23方式のentrypointへ揃えた後、4台すべてでattach入力を確認した。
- 次回: 学習者がコンソールから設定し、[試験計画書](../05_試験/試験計画書.md)へ結果を記録する。
